export type Coordinates = {
    latitude: number;
    longitude: number;
};

export type LocationData = {
    address1: string;
    city: string;
    state: string;
    postal_code: string;
    country: string;
};
